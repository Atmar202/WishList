<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/style.css">
    <title>Wish List</title>
</head>
<body>
    <div class="box">
        <% for(let i = 0; i < wishItems.length; i++) { %>
            <form action="/delete" method="POST">
                <div class="item">
            <table>
        <td><input type="checkbox" value="<%= wishItems[i].wish %>" type="checkbox" onchange="this.form.submit()"></td>
        </table>
            <p><%= wishItems[i].title %></p>
            <p class="number"><%= wishItems[i].price %></p>
        </div>
    </form>
<% } %>

        <form action="/" method="POST">
            <input type="text" name="newWish" placeholder="Add New Wish" required>
            <input type="number" name="newPrice" placeholder="Add New Price" required>
        <div class="btn">
        <button type="submit">+</button>
    </div>
        </form>
    </div>
</body>
</html>